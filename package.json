{
  "name": "arena-duel-client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Arena Duel — browser game client (TypeScript + canvas)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
