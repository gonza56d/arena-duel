# config

Setup and configuration — env vars, flags, how to run the project.

## The server refuses to start unless JWT_SECRET is set; other env vars are PORT (default 80…

What: The server refuses to start unless JWT_SECRET is set; other env vars are PORT (default 8080), MONGO_URI, MONGO_DB · Why: — · Where: light-backend/internal/config/config.go, light-backend/.env.example <!-- id: 8be8faed-7ee8-48da-9741-541435585adf-6 -->
